import type { Meta } from '@storybook/react';
import CloseDialogButton from './CloseDialogButton';

const meta: Meta<typeof CloseDialogButton> = {
  component: CloseDialogButton,
  tags: ['autodocs'],
};

export const Primary = { args: {} };

export default meta;
